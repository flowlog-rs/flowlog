// Level B — transitive closure over edges.
// Includes c.dl for the EDB; included by a.dl.

.include "c.dl"

.decl tc(x: int32, y: int32)
tc(x, y) :- edge(x, y).
tc(x, y) :- tc(x, z), edge(z, y).
.output tc
