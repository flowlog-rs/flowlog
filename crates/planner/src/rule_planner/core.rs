use super::RulePlanner;
use catalog::Catalog;
use optimizer::PlanTree;

impl RulePlanner {
    pub fn core(&mut self, _catalog: &mut Catalog, plan_tree: &PlanTree) {
        todo!("Implement the core planning phase");
    }
}
