.in
.decl NullEdge(x: integer, y: integer)
.input NullEdge.csv

.decl Edge(x: integer, y: integer)
.input Edge.csv

.out 
.decl NullNode(x: integer, y: integer)

.rule
NullNode(x, y) :- NullEdge(x, y).
NullNode(x, y) :- NullNode(x, w), Edge(w, y).

