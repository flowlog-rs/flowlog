.in
.decl Arc(a: integer, b: integer)
.input Arc.csv

.out
.decl CC2(cc: integer)
.decl CC(node: integer, cc: integer)

.rule
CC(node, min(node)) :- Arc(node, _).
CC(node, min(cc)) :- Arc(other, node), CC(other, cc).
CC2(node, min(cc)) :- CC(node, cc).
